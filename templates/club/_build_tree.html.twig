{% if build_tree is defined %}
    <p><a href="{{ path('index') }}">Logo Maison</a>
        {% for cat in build_tree %}
            / <a href="{{ path('index_forum', {'id': cat.id}) }}">{{ cat.libelle }}</a>
        {% endfor %}
    </p>
{% endif %}